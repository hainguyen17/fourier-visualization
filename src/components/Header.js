import { Navbar } from "react-bootstrap"

export const Header = () => {
    return (
        <Navbar bg="light" expand="lg">
            <Navbar.Brand>Fourier Visualizer</Navbar.Brand>
        </Navbar>
    )
}